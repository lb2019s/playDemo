// const webpack = require('webpack')
// const config = require('../webpack.config')

// const compiler = webpack(config)

// Reflect.ownKeys(compiler.hooks).forEach(hookName => {
//     compiler.hooks[hookName].tap("xxxx", () => {
//         console.log("run ==========> ", hookName);
//     })
// })

// compiler.run()